<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <nav class="flex justify-center bg-zinc-900 space-x-9">
        <a href="/dashboard"
            class="font-bol px-3 py-2 text-slate-700 rounded-lg hover:bg-slate-100 hover:text-slate-900">Home</a>
        <a href="/team"
            class="font-bol px-3 py-2 text-slate-700 rounded-lg hover:bg-slate-100 hover:text-slate-900">Team</a>
        <a href="/projects"
            class="font-bol px-3 py-2 text-slate-700 rounded-lg hover:bg-slate-100 hover:text-slate-900">Projects</a>
        <a href="/reports"
            class="font-bol px-3 py-2 text-slate-700 rounded-lg hover:bg-slate-100 hover:text-slate-900">Reports</a>
    </nav>
    <div class="bg-zinc-800">
        <br>
        <h2 style="font-size: 2em;" align="center">Sticky e-notes</h2>
    </div>
    <div class="main w-full min-h-screen bg-zinc-800">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block w-full rounded-md px-5 py-2 bg-zinc-900" type="text" name="title" id=""
                    placeholder="Title Gose here">
                <textarea class="block w-full rounded-md mt-2 px-5 py-2 resize-none bg-zinc-900" name="details" id=""
                    placeholder="Description Here" cols="30" rows="10"></textarea>
                <input type="submit" class="mt-2 bg-yellow-600 px-5 py-2 rounded-md" value="Create task">
            </form>
        </div>

        <div class="tasks flex flex-wrap gap-3 p-10 justify-center">
            <% if(files.length> 0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 bg-zinc-700 rounded-md">
                        <h2 class="text-white text-3xl tracking-tighter">
                            <%= val %>
                        </h2>
                        <div class="flex w-full justify-between item-center mt-3">
                            <a href="/file/<%= val %>" class="text-blue-600 inline-block">Read More...</a>
                            <a class="text-green-600" href="/edit/<%= val %>">Edit Filename</a>
                        </div>
                    </div>
                    <% }) %>
                        <%} else { %>
                            <h3> No Tasks Yet ! Create Some Tasks...</h3>
                            <% }%>
        </div>
    </div>
</body>

</html>